<view wx:if="{{isShow}}">
  <view class="container">
    <view class="title">温馨提示</view>
    <view class="content">{{content}}</view>
    <view class="btn" bindtap="ok">确定</view>
  </view>
  <view class="mask"></view>
</view>